<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.dao.DepartmentDao">
    <!--<select id="selectDepartmentBypage" resultType="com.example.demo.data.Department">-->
        <!--select * from (-->
        <!--select row_.*, rownum rownum_ from-->
        <!--(select * from Department-->
        <!--order by rownum asc )-->
        <!--row_ )-->
        <!--<![CDATA[-->
        <!--where rownum_ <= #{end}  and rownum_ > #{begin}-->

        <!--]]>-->
    <!--</select>-->
    <select id="selectDepartmentBypage" resultType="com.example.demo.data.UserInfo" parameterType="Map" >
        SELECT *
        FROM USER_INFO
        <if test="a!=null">
            <where>
                <if test="name!=null and name!=''"> name=#{name}</if>
                <if test="age!=null and age!=''">AND  age=#{age}</if>
            </where>
            LIMIT #{a},#{b}
        </if>
    </select>


</mapper>